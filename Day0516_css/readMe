<요소의 배치방법>
block-level : 요소가 가로 영역을 모두 차지, 같은 가로 영역에 다른 요소가 올 수 없다.
inline-level : 요소가 요소의 크기만큼만 영역을 차지, 같은 가로 영역에 다른 인라인 요소가 올 수 있다.

<배치 변경방법>
display 속성 이용하기 
 : block, inline, inline-block 값을 이용해서 
       요소들의 배치 속성을 바꿀 수 있다.
       
float 속성 이용하기
 : 요소를 화면에 떠 있는 것 처럼 표시하는 방법
      요소를 화면의 왼쪽이나 오른쪽으로 배치
      요소의 float 속성을 추가하면 너비값이 컨텐츠를 표시할 만큼만 차지
      다른 요소가 들어올 수 있는 공간을 비워둠
      한 요소에 float속성을 지정하면 float속성을 해제하기 전 까지
      뒤따라오는 요소들은 모두 float속성을 가짐
       
position 속성을 이용한 요소 배치
 : 웹 문서안 요소들을 원하는 위치에 배치할 수 있게 해주는 속성
 static : 요소들이 문서의 배치 흐름에 따라 배치가 됨
 relative : 문서의 배치 흐름에 따라 배치 하되, 위치를 지정할 수 있음
 absolute : 문서 배치 흐름과는 상관없이 원하는 위치를 지정해서 배치
 			부모요소(부모요소의 position이 relative 이면서 가장 가까운 부모)의 내부위치
 fixed : 지정한 위치에 고정 배치
 
 레이아웃 만들기
 화면을 구성하는 요소들을 배치하기 위한 전반적인 모양
 