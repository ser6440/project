html 요소는 두 가지로 분류
block 요소: 화면의 가로영역을 모두 차지하는 요소
in-line 요소: 내용만큼만 영역을 차지하기 때문에 옆으로 다른 요소가 올 수 있음

<h> : 문서내에서 제목을 표시할 때 사용, 1~6까지 중요순으로 사용
<p> : 문서내에서 문장의 단락을 만드는 역할
<b>,<strong> : 텍스트 진하게(bold)
<mark> : 특정 텍스트 강조
<blockquote> : 인용문 작성시 사용
<span> : 요소내에서 특정 영역을 지정할 때 사용
<i>,<em> : 이텔릭체
<form> : 사용자로 부터 값을 입력받고 서버로 전송하기 위한 태그
                       하위 태그로 <input> 태그를 가지면서 정보를 입력받을 수 있다.
         <input type = "종류">
         type 종류 : 
	         text : 문자열
	         password : 문자열, 가려져서 화면 출력
	         email : text와 동일인데 입력패턴을 검사해서 email주소인지 아닌지 검사
	         submit : 버튼, form 하위 요소의 내용을 서버로 전송하는 버튼, 
	                  form 요소의 action에 선언된 요청이 실행
	         button : 버튼 모양의 요소가 출력
	         reset : 버튼, form 하위 input 요소의 내용을 모두 비움
	         checkbox : 항목 출력, 다중선택 가능한 체크박스
	         radio : 항목 출력, 항목중 하나만 선택가능한 체크박스
	         file : 파일 업로드를 위해서 파일 선택 가능한 요소
	         number : 숫자입력(최대,최소 등을 지정할 수 있다.)
	         date : (날짜 입력 요소)
         
margin:다른요소와의 거리         
         
         
         